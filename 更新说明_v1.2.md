# 标讯自动化 Skill · 更新说明 v1.2

| 项 | 内容 |
|:---|:---|
| **版本** | **v1.2** |
| **release_id** | `qianlima-bid-daily-workflow_20260722_v1.2` |
| **状态** | `released` |
| **发布日期** | 2026-07-22 |
| **相对上一正式版** | **v1.1**（2026-07-20） |

> 本文件随包分发。更细总账见根目录 `改动说明.md`。

---

## 1. 本版要解决什么

1. **zip 等附件**经千里马 STREAM 下载后，本地判定「非有效 ZIP 容器」时：  
   - 业务 JSON 丢失 `source_url` → Excel 无超链接 → **Phase34 模板校验 error 阻断日报**。  
2. 生产 `run_id=20260722_083050` 即因此失败；续跑前已在 D 盘临时修复，本版 **回写权威源并加固**。

---

## 2. 代码变更（相对 v1.1）

| 文件 | 变更 |
|:---|:---|
| `scripts/qianlima_cdp_body_attachment_reader_20260624_v05.py` | 下载失败 fallback：**保留** stream/click 的 `source_url`/`real_url`；`stream_result`/`click_result` **显式初始化**（不用 `in dir()`） |
| `scripts/validate_excel_template_contract_20260603_v02.py` | `download_ok=False` 时附件 URL 问题 **降为 warning**，不阻断日报 |

**仍包含 v1.1：** 全排除 MergedCell、`all_reject` 空项目 sheet、工作区目录/记录规范等。

**生产验证（摘要）：**

- 2026-07-22 修复后 Phase34 续跑：归档 + 钉盘 + 钉钉成功。  
- 源码与 D 盘生产 scripts 已哈希对齐后打本包。

---

## 3. 已知限制

- zip **内容**仍可能无法当标准压缩包解析（加密/包装）；OCR/清单证据可能仍缺 → 依赖 needs_review / 人工打开链接。  
- 本版解决的是 **「空链接硬挡 Phase34」**，不是「任意 zip 一定能解压成功」。

---

## 4. 分发包

| 包 | 目录（发布机） |
|:---|:---|
| 完整 Skill | `01_正式发布\输出\01_完整版_当前可分发\` |
| 薄封装 | `01_正式发布\输出\02_薄封装_当前可分发\` |

薄封装仍依赖完整包 `scripts`（`QLM_BID_SCRIPTS` 等）。

---

## 5. 安装注意

1. `verify_release_manifest --root <skill根>` → ok、released。  
2. `prepare_runtime` 刷新生产 `03_脚本工具`（或等价同步本版 scripts）。  
3. 敏感配置仍在包外；勿把 webhook 写进任务描述。

---

*生成日期：2026-07-22*
