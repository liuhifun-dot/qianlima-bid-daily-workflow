# 测试任务待办 — 发布包全量复测（2026-07-14 第二轮）

| 字段 | 内容 |
|:---|:---|
| **包路径** | `G:\Codex Project\标讯自动化\01_正式发布\qianlima-bid-daily-workflow_20260706_v07-release01_source\qianlima-bid-daily-workflow\` |
| **测试 runtime** | `G:\Codex Project\标讯自动化\_test_runtime_e2e_full\` |
| **本文件位置** | `改动说明\测试任务_待办_全量复测_20260714.md` |
| **改动总账** | 上一级 `改动说明.md` |
| **本轮原始输出** | `改动说明\_retest_20260714b\` |
| **上一轮输出** | `改动说明\_retest_20260714\` |
| **复测时刻** | 2026-07-14 约 16:47–16:50（本机） |
| **T0 执行** | 2026-07-14 约 17:05 — **preflight 已绿**（见下「T0 结果」） |

> **结论先看：**  
> - **T0 已完成**：`full_run_ready=true`，U1/U6（本测试 runtime + 源码包 verify）已关闭。  
> - **仍开放**：U2 附件下载质量、U7 全流未重跑、U8 backup 正式发一次未验。  
> - 代码侧 S1–S5 / 附件 P0 看门狗单测与冒烟通过；**不能**视为业务零缺陷。  
> - 下方第三部分是你的**逐项复测清单**；T0 勾选区可标完成。

---

## 第一部分：本轮已做的复测（Agent）

### 1.1 结果总览

| 测试项 | 结果 | 证据文件 | 说明 |
|:---|:---:|:---|:---|
| 关键脚本 `py_compile`（10 个） | **PASS** | `_retest_20260714b\01_py_compile.txt` | 含 export/pipeline/attach/ocr/lock 等 |
| 源码包 `verify_release_manifest` | **FAIL** | `02_verify_manifest.txt` | 10 个 SHA 不一致 + 大量 unlisted（见 U6） |
| V1 额度解析 0/1/空/冒号 | **PASS** | `06_unit_v1v2.json` | `extract_quota_remaining` |
| V2 soft-continue 四案 | **PASS** | `06_unit_v1v2.json` | 4/6 放行、1/6 拒、非 body_empty 拒、flag 关拒 |
| V3 CDP 锁 acquire/stale/release | **PASS** | `03_unit_tests.json` | port 19301 |
| V4 附件常量 + SegmentLogger + OCR PROGRESS | **PASS** | `03_unit_tests.json` | DL=90 / OCR hard=1800 / skip=100MB / 项目预算 420 |
| V5 fallback 开关存在于 pipeline | **PASS** | 源码扫描 | `--allow-existing-export-download` |
| ensure_browser CDP 9222 | **PASS** | `04_ensure_browser.txt` | 登录态有效 |
| runtime_preflight（带本地/共享归档） | **FAIL** | `05_preflight.txt` | 见 U1；其余配置/CDP/归档路径 OK |
| 附件脚本冒烟 `--max-projects 1` | **部分 PASS** | `11_attach_smoke_segment.log` | 见 1.2 |
| D 盘关键脚本哈希 vs 发布包 | **PASS** | `08_d_disk_hash.txt` | 4 个核心脚本**一致**（相对此前 U5 已对齐） |
| 完整 P0→P4 再跑一轮 | **未做** | — | 额度/时长；请用 T8 |

### 1.2 附件看门狗冒烟（本轮重跑）

| 项 | 内容 |
|:---|:---|
| **命令** | `qianlima_cdp_body_attachment_reader_20260624_v05.py --max-projects 1 --cdp http://localhost:9222` |
| **输入** | `output\v2_4\标讯筛选结果_20260714_v03.json` → 项目 `bid-613407915` |
| **run** | `_test_runtime_e2e_full\runs\retest_watchdog_20260714b\` |
| **分段日志** | `...\99_logs\phase2c_attach_20260714_164917.log` |
| **用时** | 约 **57s**，EXIT=0，**未卡死** |
| **正文** | `body_ok=True` |
| **附件** | 检出 3 个；**`attachment_read_ok_count=0`**（0/3） |

**日志中的具体报错/失败点：**

```text
[SPA] signal downloads=0 responses=2 t=1s
[SPA] fail after 1s
...
[PREVIEW] stall no state change for 45s
[PREVIEW] timeout/stall after 46s
[PROJECT] end bid=bid-613407915 attach_status=unreadable ok=0/3 elapsed=57.3s
```

**结论：**

- P0「不静默卡死 + 有 `[PROJECT]/[ATTACH]/[SPA]/[PREVIEW]` 分段日志」→ **验证通过**
- 「附件一定能下到真文件并可读」→ **仍失败**（质量问题 U2，与「卡死」不同）

### 1.3 preflight 其它检查（哈希失败之外）

| 检查 | 结果 |
|:---|:---|
| Python 3.12 + 依赖 | OK，无缺包 |
| Chrome | 找到 |
| 敏感配置账号/密码/主 webhook | 均 present |
| **backup webhook** | **present / configured=true** |
| dws 可执行 | working；`auth_checked=false`（未做上传鉴权深检） |
| 本地/共享归档目录（本轮参数传入） | configured + exists |
| CDP localhost:9222 | reachable + 登录 hint + 千里马 tab=1 |
| Kimi 通道 | 未起（当前 CDP 主路径可接受） |

**blocked 唯一正式原因：**

```text
Runtime release identity/hash verification failed.
```

涉及 7 个 runtime 脚本 actual≠expected（`bid_export_auto` / `qianlima_cdp_*` / `gen_report` / `run_daily_pipeline` / `runtime_preflight` / `prepare_runtime` / `ocr_pdf`）。  
根因：脚本已按新代码拷入 runtime，但 **`runtime_release_manifest_*.json` 仍是旧哈希**，且源码包 `release-manifest.json` 也未重算。

### 1.4 历史 E2E 残留（非本轮重跑）

| Run | 范围 | 结果要点 |
|:---|:---|:---|
| `e2e_p2p4_20260714_094224` | 2a→34 | pipeline `status=ok`；日报/归档/钉盘/主钉钉有产物 |
| 同 run 证据校验 | 2b 校验 | `body_read_ok=4/6`，`body_empty=2`，`attachment_read_ok=0/3`，`systemic_attachment_failure=true`；靠 soft-continue 放行 |
| 同 run 钉钉 | 34 | 主 webhook ok；**当时** `backup_webhook_configured=false`；抄送为**事后手工**（`钉钉抄送结果_backup_webhook_20260714.json`） |
| Phase1 正式新导出 | — | 额度 0 路径仍依赖站点；fail-fast/fallback 靠代码 |

### 1.5 issue-log 状态快照

| ID | 状态 | 说明 |
|:---|:---:|:---|
| attach-download-watchdog | mitigated | 不卡死已验证；下载成功率未解 |
| quota-zero | mitigated | fail-fast + 可选 fallback |
| cdp-contention | resolved | 等锁 5–10 分钟单测 OK |
| dingtalk-backup | resolved（代码） | 配置已 present；**正式跑一次 backup 成功**仍建议你勾 T4 |
| soft-continue | mitigated | 单测 OK；业务上仍可能 needs_review 偏多 |
| attach-rename | resolved | WinError 183 幂等 |

---

## 第二部分：仍未修复 / 仍会报错的内容

| # | 问题 | 状态 | 报错/现象位置 | 说明 |
|:---:|:---|:---:|:---|:---|
| **U1** | runtime SHA256 与 manifest 不一致 | **已关闭（T0）** | 原 preflight blocked | 已 `prepare_runtime --update`；现 `runtime_release.ok=true` |
| **U2** | 附件下载成功率仍低 | **已缓解（2026-07-14）** | 原 0/3；现 stream **5/5 download_ok** | fileItem+ZBFileStream；见 `u2_results.json`；外链-only 仍待回归 |
| **U3** | body_empty 个案 | mitigated | 历史 vip body_empty | soft-continue 可放行 |
| **U4** | 额度 0 新导出 | mitigated | 站点额度 | fail-fast + 可选 fallback |
| **U5** | D 盘生产脚本偏旧 | **已对齐** | `08_d_disk_hash.txt` | 定时任务路径仍建议 T10 再确认 |
| **U6** | 源码包 release-manifest 过期 | **已关闭（T0）** | 原 verify FAIL | 已重算 66 文件 + ignore `改动说明/_retest_`；verify **ok**；backup：`release-manifest.json.bak_t0_20260714` |
| **U7** | 完整 P1→34 本轮未重跑 | 待你测 | — | 见 T8 |
| **U8** | backup 正式链路未 E2E 验证 | **已关闭（T4）** | 主+backup errcode=0 | 见 T4 测试消息 |

### T0 执行结果（2026-07-14 17:05）

| 步骤 | 结果 | 证据 |
|:---|:---:|:---|
| 重算 `release-manifest.json` | PASS | `t0_01_rebuild_manifest.json`；release_id `…-t0hash` |
| 源码包 `verify_release_manifest --allow-release-candidate` | PASS | `t0_02_verify.json` → `ok:true` |
| `prepare_runtime --update --allow-release-candidate` | PASS | runtime 27 files 同步；含 `cdp_run_lock` |
| preflight（静态，带归档路径） | PASS | `status=static_checks_passed`，`ok=true`，**无 blocked** |
| preflight **`--check-online`** | **PASS** | `t0_05_preflight_online.json`：`status=ready`，**`full_run_ready=true`** |

在线预检要点：CDP 已登录、dws auth_ok、双归档路径存在、backup webhook 已配置、runtime 哈希 0 错误。

### T5 执行结果（2026-07-14 17:16）

| 案 | 内容 | 结果 |
|:---|:---|:---:|
| A | 真实 E2E 校验 4/6 body_empty → `_soft_continue_allowed=True` | PASS |
| B | body_ok=1/6 → False | PASS |
| C | `no_soft_continue=True` 时 4/6 也 False | PASS |
| D | 非白名单 need_login → False | PASS |
| E | pipeline 分支 mock CDP：日志出现 `[soft-continue] ratio=4/6` | PASS |
| F | pipeline + no_soft：`[soft-continue-denied]` + PipelineError | PASS |
| G | pipeline 低比例：denied + 失败 | PASS |

证据：`改动说明/_retest_20260714b/t5_results.json`  
说明：T5 用**历史真实校验 fixture** + **pipeline 同函数路径**验证决策；未再全量 6 项目实机 CDP（由 T6 覆盖实机 2b）。

### T6 执行结果（2026-07-14 17:17–17:20）

| 检查项 | 结果 |
|:---|:---|
| 入口 | `run_daily_pipeline --start-phase 2b --end-phase 2c`，缩样 screening（considered+low 各 1 → 共 2 项目） |
| run_id | `t6_watchdog_20260714_171710` |
| **分段日志** | **有** `99_logs/phase2c_attach_20260714_171716.log` 与 `_171903.log`（attempt 1/2 各一份） |
| 标签 | `[PROJECT]`/`[ATTACH]`/`[SPA]`/`[PREVIEW]` 均有；本次无成功下载故无 `[DL]`/`[OCR]` |
| 看门狗 | PREVIEW stall 45s 后退出；SPA fail after 1s；**未静默卡死**；约 3.5 分钟整段结束 |
| 附件质量 U2 | 仍 **0 成功**（`attach_status=unreadable ok=0/3` 等） |
| pipeline 退出 | **exit=1**：小样本 input=2 且 body_ok=1 → **soft-continue-denied**（符合 T5 小样本规则，非看门狗失败） |
| 副作用 | 失败告警曾发到钉钉（pipeline 失败告警） |

证据：`_retest_20260714b/t6_summary.json`、`t6_segment.log`、`t6_pipeline_stdout.txt`  
**T6 结论：** P0「有分段日志 + 不卡死 + stall 可退出」**通过**；「附件可读成功」**未通过**（U2 仍 open）。

### T3 执行结果（CDP 等锁）

| 案 | 内容 | 结果 |
|:---|:---|:---:|
| A | acquire / release | PASS |
| B | 持锁进程占住 → 等待进程打印 waiting → 约 7s 后 acquire | PASS |
| C | 死 PID stale lock 回收 | PASS |
| D | wait 超时 → 清晰 TimeoutError（含 holder pid/run_id） | PASS |
| E | pipeline `max(300, wait)` 下限 5 分钟 | PASS |

- 测试端口 **19334**（**未占用**生产 9222）  
- 证据：`改动说明/_retest_20260714b/t3_results.json`  
- **说明：** 失败告警保持开启（你的要求：网页改版/流程失败要能看见）；本项未改告警逻辑。

### T4 执行结果（钉钉 backup）

| 检查 | 结果 |
|:---|:---|
| 主 webhook 配置 | true |
| backup 配置 | true |
| 主发送 | success，errcode=0 |
| backup 发送 | success，errcode=0 |
| dry_run | backup `skipped_reason=dry_run`（不发） |

- 群里应看到标题类似 **`[标讯自动化·测试] T4 双 webhook 验证`** 的测试 markdown（主群 + 抄送各一条）  
- 证据：`t4_results.json`；`runs/t4_backup_webhook_test/07_archive_push/钉钉发送结果_T4_backup_test.json`  
- **未打印** webhook URL/token  
- **U8（backup 未在配置后验证）→ 关闭**

**本轮确认已缓解/通过的代码能力：**

- 额度 0 fail-fast 解析、CDP 单写锁、soft-continue 比例与白名单、fallback 开关、rename 幂等、OCR 心跳常量、附件**分段日志 + stall/预算（P0 不卡死）**、backup 配置可被 preflight 识别。

---

## 第三部分：你的逐项复测清单

> **环境建议**  
> - 配置：`G:\Codex Project\本机敏感配置\本机敏感配置.md`  
> - Runtime：`_test_runtime_e2e_full`（**先做 T0 刷新哈希**）  
> - CDP：`http://localhost:9222` 专用 Chrome  
> - 工具：Python 3.12、Chrome、可选 `dws`、PowerShell  

---

### T0. 发布前/预检（必做）

| | |
|:---|:---|
| **步骤** | 1）在源码包执行：`python scripts/prepare_runtime_20260622_v02.py --runtime-root "<runtime>" --allow-release-candidate`（或你们正式刷新流程） 2）重算/更新 `release-manifest.json` 后：`python scripts/verify_release_manifest_20260623_v01.py --root <包根> --allow-release-candidate` 3）`python 03_脚本工具/runtime_preflight_*.py --config <敏感配置> --local-archive-dir ... --share-archive-dir ...` 4）确认 `full_run_ready=true`、`ok=true` |
| **可能发现** | U1 哈希仍不一致；归档路径未配；CDP 未登录；manifest 仍有 unlisted（U6） |
| **工具** | Python、`prepare_runtime`、`verify_release_manifest`、`runtime_preflight`、敏感配置、归档盘 |

---

### T1. S1 额度 0 fail-fast

| | |
|:---|:---|
| **步骤** | 1）确认当日导出额度耗尽或人为触发「剩余额度0条」弹窗 2）`run_daily_pipeline ... --start-phase 1 --end-phase 1` 3）观察是否**约 1 分钟内**失败且文案含「剩余 0 条」/quota，**无**十几分钟空刷「待生成」 |
| **可能发现** | 弹窗文案变导致解析不到 0；仍刷满 generation timeout |
| **工具** | CDP Chrome、`bid_export_auto_v1` / pipeline；截图额度弹窗 |

---

### T2. S5 fallback 开关（默认关）

| | |
|:---|:---|
| **步骤** | A）不加 flag，额度 0 → 应硬失败  B）加 `--allow-existing-export-download`，页面有「已生成+下载」→ 应出 xlsx，且指针/`latest_qianlima_export_source.json` 中 `source=fallback_existing_export`（或等价字段） |
| **可能发现** | 下错行；指针未写；flag 未传到子进程 |
| **工具** | pipeline CLI；导出记录页；`output\` 指针与 source json |

---

### T3. S2 CDP 等锁 5～10 分钟

| | |
|:---|:---|
| **步骤** | 1）终端 A：启动 pipeline（含 1 或 2b）并保持运行 2）终端 B：立刻再启同一 `--cdp-port 9222` 的 pipeline 3）B 应打印 `[cdp_lock] waiting...` 4）结束 A 后 B 可 acquire；或 B 超过 wait 后失败文案清晰 |
| **可能发现** | 锁未释放；stale 误杀/误放；两进程仍互点浏览器 |
| **工具** | 双终端；`%TEMP%\qlm_bid_cdp_9222.lock`；pipeline |

---

### T4. S3 钉钉 backup（配置后正式验证）

| | |
|:---|:---|
| **步骤** | 1）preflight 确认 `dingtalk_webhook_backup_configured: true` 2）跑 Phase34（或 gen_report 正式发一次，注意打扰群） 3）打开 `runs/.../07_archive_push/钉钉发送结果_*.json` 确认 `backup_webhook` 成功（非仅手工抄送文件） |
| **可能发现** | backup token 失效；只发主群；full_run 被 backup 拖死（当前设计不应） |
| **工具** | 敏感配置主/备 webhook；gen_report / pipeline 34；钉钉群 |

---

### T5. S4 soft-continue

| | |
|:---|:---|
| **步骤** | 用曾含 body_empty 的筛选结果从 2b 跑： A）4/6 body_ok → 应进 2d（日志有 `[soft-continue]`） B）故意使 ratio&lt;50% → 2b 失败（`[soft-continue-denied]`） C）`--no-soft-continue` → 有 body_empty 即失败 |
| **可能发现** | 假绿；过严无法出日报；校验字段名变更导致判断失效 |
| **工具** | pipeline `--start-phase 2b`；历史 screening/vip json；`CDP证据校验_*.json` |

---

### T6. 附件 P0 看门狗 + 分段日志（重点）

| | |
|:---|:---|
| **步骤** | 1）确认 runtime 已是最新 `qianlima_cdp_body_attachment_reader_*.py` 2）`run_daily_pipeline --start-phase 2a --end-phase 2d`（或仅 2b） 3）确认存在 `runs/<run_id>/99_logs/phase2c_attach_*.log` 4）打开 log：应有 `[PROJECT]`/`[ATTACH]`/`[DL]`/`[SPA]`/`[PREVIEW]`/`[OCR]` 5）慢网/卡预览时应 **stall 退出** 而非数十分钟静默 6）可选：重放 `bid-613432521` / 标神卡死项目对照 |
| **可能发现** | 仍静默（脚本未同步）；预算过短误杀；下载仍 0 成功（U2） |
| **工具** | CDP；screening json；`99_logs`；可选对比 `D:\bid_workflow\runs\` |

**参考命令（冒烟）：**

```text
python scripts\qianlima_cdp_body_attachment_reader_20260624_v05.py ^
  --input "<runtime>\output\v2_4\标讯筛选结果_*.json" ^
  --output-dir "<run>\03_body\cdp_evidence" ^
  --date YYYYMMDD --cdp http://localhost:9222 --max-projects 1 ^
  --config "<敏感配置.md>"
```

---

### T7. OCR 进度心跳

| | |
|:---|:---|
| **步骤** | 找需 OCR 的扫描 PDF（&lt;100MB）跑 2b；日志应出现 `[OCR] progress` / heartbeat；卡住时应 stall/hard_cap 有明确 `ocr_status` |
| **可能发现** | PaddleOCR 缺依赖；大文件 &gt;100MB 被 skip；附件根本没下下来导致测不到 OCR（先解 U2 或挑已有下载成功的项目） |
| **工具** | tpk-ocr；附件下载成功的项目；`phase2c_attach_*.log` |

---

### T8. 完整 P0→P4（正式回归）

| | |
|:---|:---|
| **步骤** | 1）额度可用日 + T0 通过 2）`run_daily_pipeline --start-phase 1 --end-phase 34 --config ... --local-archive-dir ... --share-archive-dir ...` 3）检查：xlsx 指针、日报 xlsx、本地/共享归档、钉盘、主+备钉钉、`pipeline_manifest` status=ok |
| **可能发现** | 额度 0；U1 preflight 拦住；U2 附件缺口 → needs_review 偏多；钉盘/共享盘权限 |
| **工具** | 全链路：CDP、dws、归档盘、双 webhook |

---

### T9. 四类记录目录（A/B/C/D）

| | |
|:---|:---|
| **步骤** | 1）读 `references/logging-and-records.md` §0 2）确认：会话报告在 `日志/`；run 产物在 runtime `runs/`；问题在 `issue-log/`；规则在 `lessons/lessons_log.md` 3）故意失败一次：是否开了 issue、有无密钥进 md |
| **可能发现** | 仍把 pipeline_log 塞进 issue-log；密钥泄露 |
| **工具** | 文档；任意一次失败 run |

---

### T10. 生产路径再确认（运维）

| | |
|:---|:---|
| **步骤** | 1）对本轮已 match 的四个脚本再 `Get-FileHash` 一次 2）确认**定时任务实际 cwd / 脚本路径**是否就是 `D:\bid_workflow`（或你们生产目录） 3）确认任务用的是带看门狗的 reader，而非旧拷贝 |
| **可能发现** | 任务指向旁路目录；未带 `cdp_run_lock`；仍无 99_logs |
| **工具** | `Get-FileHash`；任务计划程序 / 启动脚本；备份目录 |

---

## 第四部分：建议测试顺序

```text
T0 预检/哈希（先过 full_run_ready）
  → T3 锁（可选并行）
  → T6 附件看门狗 + 99_logs（核心改动）
  → T7 OCR（有扫描件且附件能下时）
  → T1/T2 额度相关（额度日）
  → T5 soft-continue
  → T4 钉钉 backup
  → T8 全流
  → T9/T10 文档与生产路径
```

每项勾选后建议在本文件末尾「勾选区」打勾，并链到 `日志/<会话>/` 或 `runs/<run_id>/`。

---

## 第五部分：勾选区（给你）

| ID | 完成 | 结果 pass/fail | 备注 / 日志路径 |
|:---|:---:|:---:|:---|
| T0 | ☑ | **pass** | 首次 17:05；**U2 后再刷** `t0b_04_preflight_online.json` → `full_run_ready=true`（release_id `…-t0hash-u2`） |
| T1 | ☐ | | |
| T2 | ☐ | | |
| T3 | ☑ | **pass** | 双进程等锁 ~7s 后 acquire；stale reclaim；timeout 文案清晰；证据 `t3_results.json` |
| T4 | ☑ | **pass** | 主+backup 均 errcode=0；dry_run 跳过 backup；证据 `t4_results.json`（钉钉有「T4 双 webhook 验证」测试消息） |
| T5 | ☑ | **pass** | 17:16 七案全过；证据 `t5_results.json`；A 放行 4/6、B 拒 1/6、C `--no-soft-continue` 拒 |
| T6 | ☑ | **pass（P0）/ U2 仍失败** | run `t6_watchdog_20260714_171710`；`99_logs` 两份 phase2c_attach；未卡死；附件仍 0 成功 |
| T7 | ☐ | | |
| T8 | ☐ | | |
| T9 | ☐ | | |
| T10 | ☐ | | |

---

## 第六部分：本轮交付 4 点摘要

| 点 | 内容 |
|:---|:---|
| **1 改了什么** | 对发布包做第二轮全量自动化复测 + 附件冒烟；刷新本待办；写入 `_retest_20260714b/` |
| **2 原来→现在** | 复测结论与未修复列表更新（U5 D 盘哈希本轮已对齐；U1/U2/U6 仍未修） |
| **3 连锁风险** | 本轮**未再改业务脚本逻辑**；未重算 manifest；未强制 prepare_runtime |
| **4 路径** | 见文件头；原始输出 `改动说明\_retest_20260714b\`；冒烟 `runs\retest_watchdog_20260714b\` |

---

## 相关路径速查

| 用途 | 路径 |
|:---|:---|
| 本待办 | `…\qianlima-bid-daily-workflow\改动说明\测试任务_待办_全量复测_20260714.md` |
| 改动总账 | `…\qianlima-bid-daily-workflow\改动说明.md` |
| 本轮复测输出 | `…\改动说明\_retest_20260714b\` |
| 冒烟分段日志 | `…\_test_runtime_e2e_full\runs\retest_watchdog_20260714b\99_logs\` |
| 历史成功 2–34 | `…\runs\e2e_p2p4_20260714_094224\` |
| issue 总表 | `…\issue-log\INDEX.md` |
| 记录体系 | `…\references\logging-and-records.md` |
